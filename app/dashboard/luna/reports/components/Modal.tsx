// Promoted to a shared component so the Ivy dashboard can reuse the same
// drawer/dialog shell. Kept as a re-export so existing report imports work.
export { default } from '@/components/DashboardModal';
